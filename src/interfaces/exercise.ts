export interface IExercise {
  name: string;
  exerciseId: string;
  createdBy: string;
}

export interface IExerciseInformation {
  exerciseId: string;
  name: string;
  unit: string;
  weight: string;
  reps: string;
  series: string;
}
